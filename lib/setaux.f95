! ============================================
subroutine setaux(mbc,mx,xlower,dx,maux,aux)
! ============================================

    ! set auxiliary arrays 
    ! dummy routine when no auxiliary arrays

    implicit double precision (a-h,o-z)

end subroutine setaux
